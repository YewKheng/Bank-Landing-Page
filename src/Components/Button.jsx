export const Button = ({ styles }) => {
	return (
		<button
			type="button"
			className={`${styles} bg-blue-gradient text-primary font-poppins text-lg font-medium py-4 px-6 rounded-[10px] outline-none`}>
			Get Started
		</button>
	);
};
